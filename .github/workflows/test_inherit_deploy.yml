name: Deploy Application A

on:
  workflow_call:

env:
  DEPLOY_FLAG: ${{ secrets.ALLOW_DEPLOYMENT }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check if deployment is allowed
        run: |
          echo "🚀 Checking deployment permission..."
          if [ "$(printf '%s' "$DEPLOY_FLAG" | tr '[:upper:]' '[:lower:]')" = "true" ]; then
            echo "true"
          else
            echo "false"
          fi
