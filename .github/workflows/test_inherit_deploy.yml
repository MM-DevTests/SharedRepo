name: Deploy Application A

on:
  workflow_call:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check if deployment is allowed
        run: |
          echo "🚀 Checking deployment permission..."
          
          if [ "$(echo "${{ secrets.ALLOW_DEPLOYMENTS || '' }}" | tr '[:upper:]' '[:lower:]')" = "true" ]; then
            echo "true"
          else
            echo "false"
          fi
