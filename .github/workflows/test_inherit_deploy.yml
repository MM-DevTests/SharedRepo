name: Deploy Application A

on:
  workflow_call:
    outputs:
      allow:
        description: "Checks to allow deployment."
        value: ${{ jobs.deploy.outputs.allow }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    outputs:
      allow: ${{ steps.check.outputs.allow }}
    steps:
      - name: Check if deployment is allowed
        id: check
        env:
          ALLOW_DEPLOYMENT: ${{ secrets.ALLOW_DEPLOYMENT }}
        run: |
          if [ -z "$ALLOW_DEPLOYMENT" ]; then
            echo "allow=false" >> "$GITHUB_OUTPUT"
          elif [ "$ALLOW_DEPLOYMENT" = "true" ]; then
            echo "allow=true" >> "$GITHUB_OUTPUT"
          else
            echo "allow=false" >> "$GITHUB_OUTPUT"
          fi
